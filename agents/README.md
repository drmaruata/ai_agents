# Agent Roles

The production prompts for the five agents will live under `agents/<name>/system.md` and must remain aligned with the architecture specification.

The initial scaffold defines role boundaries before adding model-specific adapters.
