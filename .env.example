# Cloud control plane
ENVIRONMENT=development
API_HOST=127.0.0.1
API_PORT=8000

# Dashboard
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Local bridge
RUATA_CONTROL_PLANE_URL=ws://localhost:8000/ws/bridge
RUATA_DEVICE_ID=
RUATA_DEVICE_TOKEN=

# Model provider configuration is intentionally abstracted for the scaffold.
MODEL_PROVIDER=
MODEL_NAME=
MODEL_API_KEY=

# Database (add a real secret only in local secure storage / deployment environment)
DATABASE_URL=
